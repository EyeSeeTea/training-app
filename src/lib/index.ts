// Public entrypoint for library builds.
//
// Important: keep this surface independent from `src/webapp/*` (app-only code).
// Export only the reusable tutorial module API.
export * from "../tutorial-module";
